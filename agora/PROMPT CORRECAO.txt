O botão "Baixar PDF" está mostrando "Não foi possível gerar o PDF no momento.".

Corrija isso mostrando o erro real nos logs, sem expor para o usuário. Verifique app/pdf.py e o trecho do download_button em app/streamlit_app.py.

Confirme se reportlab está no requirements.txt da raiz e no requirements.txt da pasta agora, se houver os dois.

Gere localmente um PDF de exemplo com um BO real e corrija qualquer erro de ReportLab, acentuação, tabela ou campo nulo.

Depois execute:
python -m compileall app agente db tests -q
python -m pytest -q